# 核心依赖（云端部署必需）
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
openai==1.3.0
requests==2.31.0
pydantic==2.5.0
python-multipart==0.0.6
httpx==0.25.0

# 注意：云端部署使用 AI Builder 的云端 STT API
# 以下本地依赖已移除以适配 256MB RAM 限制：
# - faster-whisper（本地语音转录）
# - deepgram-sdk, google-cloud-speech（备选云端 API）
# - librosa, pydub（音频处理）
# - pynput, sounddevice, soundfile, numpy（本地录音功能）
# 如需本地开发完整功能，请使用 requirements-local.txt

